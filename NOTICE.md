# Notice

`shutup-mcp` is an experimental alpha tool-list filtering proxy for MCP.

It filters tool visibility based on explicit user or client-provided intent.

It does not enforce authorization, sandboxing, data-loss prevention, or policy compliance.

Do not use untrusted MCP server configurations.
